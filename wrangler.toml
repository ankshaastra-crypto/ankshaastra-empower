name = "ankshaastra-empower"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
compatibility_flags = [ "nodejs_compat_v2" ]

# NOTE: D1 database binding is configured via Cloudflare Dashboard:
# Pages → ankshaastra-empower → Settings → Functions → D1 Database Bindings
# Variable name must be: DB
# Do NOT add [[d1_databases]] here — Pages ignores it and it causes deployment errors.

# ─── Build-time variables (Vite bakes these into the frontend bundle) ──────────
# These MUST also be set in Cloudflare Pages Dashboard →
# Settings → Variables & Secrets → for them to work at BUILD time.
[vars]

VITE_SUPABASE_URL = "https://hhibpjjozfrtyemivgqz.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY = "sb_publishable_lUUcU7jfgfV9h2RIH041lA_L3npqtRn"

# NOTE: All SECRET values (API keys, passwords, secrets) must be added via
# Cloudflare Pages Dashboard → Settings → Variables & Secrets → Add Secret
# Do NOT put real secret values in this file (it gets committed to git).

SMTP_PORT = "587"
SMTP_SECURE = "false"
FROM_EMAIL = "madappscreator@gmail.com"
ADMIN_EMAIL = "madappscreator@gmail.com"
VITE_RAZORPAY_KEY_ID = "rzp_live_SiOfLiV0lwCQ7H"
WHATSAPP_ADMIN_NUMBER = "919667305577"

name = "ankshaastra-empower"

[[d1_databases]]
binding = "DB"
database_name = "ankshaastra-db"
database_id = "d801cb24-4f5e-439a-9615-d4bf89545cce"

PACKAGE_SINGLE_PRICE = 2447
PACKAGE_PREMIUM_PRICE = 8927
PACKAGE_NAMECHECK_1_PRICE = 293
PACKAGE_NAMECHECK_2_PRICE = 528
PACKAGE_NAMECHECK_3_PRICE = 747
PACKAGE_CONSULTATION_PRICE = 1