name = "ankshaastra-empower"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
compatibility_flags = [ "nodejs_compat_v2" ]

# ─── Build-time variables (Vite bakes these into the frontend bundle) ──────────
# These MUST also be set in Cloudflare Pages Dashboard →
# Settings → Variables & Secrets → for them to work at BUILD time.
[vars]

VITE_SUPABASE_URL = "https://hhibpjjozfrtyemivgqz.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY = "sb_publishable_lUUcU7jfgfV9h2RIH041lA_L3npqtRn"

# NOTE: All SECRET values (API keys, passwords, secrets) must be added via
# Cloudflare Pages Dashboard → Settings → Variables & Secrets → Add Secret
# Do NOT put real secret values in this file (it gets committed to git).

SMTP_SECURE = "false"
FROM_EMAIL = "Ankshaastra <no-reply@ankshaastra.com>"
ADMIN_EMAIL = "social@ankshaastra.com"

WHATSAPP_ADMIN_NUMBER = "919667305577"